# JWT_SECRET: Secure secret key used to sign and verify JSON Web Tokens (JWT) for authentication.
JWT_SECRET="enterprise-super-secret-key-2026"

# PORT: Port where the backend server runs (Defaults to 3000)
PORT=3000

# GEMINI_API_KEY: Optional - Only required if using Gemini AI API services
GEMINI_API_KEY=""

